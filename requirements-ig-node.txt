# Python Dependencies for IG Node
# PAC-INFRA-P1502: IG Node Infrastructure Build & Deployment
#
# CLASSIFICATION: LAW-tier
# AUTHORITY: DAN [GID-07]
# VERSION: 1.0.0
# DATE: 2026-01-25

# Core dependencies (minimal for security)
# No AI/ML libraries (IG does NOT optimize, only enforces)

# HTTP client (for health checks, ledger writes)
requests==2.31.0

# PostgreSQL client (epistemic independence - direct DB access)
psycopg2-binary==2.9.9

# JSON Web Tokens (Ed25519 signature verification)
PyJWT[crypto]==2.8.0

# Cryptography (Ed25519 signatures for Dissent Objects)
PyNaCl==1.5.0

# YAML parsing (for configuration)
PyYAML==6.0.1

# Prometheus metrics client
prometheus-client==0.19.0

# Structured logging
python-json-logger==2.0.7

# ============================================================================
# Constitutional Note:
# ============================================================================
# The IG Node has MINIMAL dependencies. No AI/ML libraries (no TensorFlow,
# PyTorch, scikit-learn). The IG does NOT optimize or suggest. It ONLY enforces
# compliance mechanically via OPA policies. This prevents IG feature creep
# and maintains the Negative Constitution (I-GOV-010).
# ============================================================================
