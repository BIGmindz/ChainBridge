# DEPRECATED AGENT PROMPTS

This folder is **deprecated** and kept only for historical reference.

Canonical agent prompts, checklists, and knowledge scopes now live in:

- `AGENTS 2/`

Rules:

- Do **not** add new files under `AGENTS/`.
- Do **not** update prompts here.
- Always edit the corresponding file under `AGENTS 2/`.

See `AGENT_FRAMEWORK_COMPLETE.md` for the current agent standard.
