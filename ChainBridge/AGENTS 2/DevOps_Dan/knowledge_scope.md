Scope:
- CI/CD pipelines (`.github/workflows/**`)
- Container/build artifacts (`Dockerfile*`, `docker-compose.yml`)
- Infra manifests (`k8s/**`)
- Make targets and dev scripts (`Makefile*`, `scripts/dev/**`)
